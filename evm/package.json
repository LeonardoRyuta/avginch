{
  "name": "icp-fusion-evm-contracts",
  "version": "1.0.0",
  "description": "EVM contracts for ICP Fusion cross-chain atomic swaps",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:local": "hardhat ignition deploy ignition/modules/ICPEscrow.ts --network localhost",
    "deploy:base-sepolia": "hardhat ignition deploy ignition/modules/ICPEscrow.ts --network base-sepolia --verify",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "hardhat": "^2.26.1",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
